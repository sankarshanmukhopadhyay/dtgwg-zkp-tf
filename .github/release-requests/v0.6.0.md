# Release request: v0.6.0 reconciliation

Reconcile the existing `v0.6.0` GitHub Release metadata against the corrected `RELEASE_NOTES_v0.6.0.md` on the current merged `main` baseline.

The workflow is idempotent: it must not duplicate the release or move the existing tag. It should only validate, reconcile title/body/latest status, and verify the published release.
